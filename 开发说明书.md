# smart_learning 后端开发说明书

## 1. 项目目标

本项目是一个 smart_learning 学习推荐网站，后端主要负责从 MySQL 数据库读取专业、课程、知识点、职业、职业能力和职业知识点等数据，并提供给前端页面展示。

核心业务链路：

```text
专业 major
-> 专业课程关联 major_course
-> 课程 course
-> 课程知识点 knowledge_point

职业 career
-> 职业课程关联 career_course
-> 职业能力 career_ability
-> 职业知识点关联 career_knowledge_point
```

第一阶段后端目标不是做复杂推荐算法，而是先把数据库中的结构化数据稳定查出来。

## 2. 当前数据库状态

数据库名：

```text
smart_learning
```

当前主要表：

```text
major
course
major_course
knowledge_point
career
career_course
career_ability
career_knowledge_point
course_backup_before_rebuild
```

已确认的核心数据量：

```text
major: 3
course: 63
major_course: 89
knowledge_point: 129
career: 17
career_course: 159
career_ability: 43
career_knowledge_point: 65
```

当前数据库可以支撑后端第一阶段开发。

已知未补全数据：

```text
部分职业没有 career_ability
部分职业没有 career_knowledge_point
career_profile 暂未导入
major_course.credits 当前为空
```

后端接口必须兼容这些缺失数据。缺失列表应该返回空数组，不要抛异常。

## 3. 开发环境建议

建议在 IDEA 中完成后端开发。

推荐配置：

```text
JDK: 11
Spring Boot: 2.7.x
构建工具: Maven
数据库: MySQL 8.0
持久层: MyBatis
```

建议依赖：

```text
Spring Web
MyBatis Framework
MySQL Driver
Lombok
```

如果 IDEA 使用 Spring Initializr 创建项目，建议配置：

```text
Group: com.example
Artifact: smart-learning-backend
Package name: com.example.smartlearning
Packaging: Jar
Java: 11
```

## 4. 后端目录结构建议

建议后端目录放在 `backend` 下。

```text
backend
└── src
    └── main
        ├── java
        │   └── com.example.smartlearning
        │       ├── SmartLearningApplication.java
        │       ├── common
        │       ├── config
        │       ├── controller
        │       ├── dto
        │       ├── entity
        │       ├── mapper
        │       ├── service
        │       ├── service.impl
        │       └── vo
        └── resources
            ├── application.yml
            └── mapper
```

各目录作用：

```text
common: 通用返回结果、常量、工具类
config: 配置类
controller: 接收前端请求
dto: 接收前端提交的数据
entity: 数据库实体类
mapper: MyBatis Mapper 接口
resources/mapper: MyBatis XML SQL 文件
service: 业务接口
service.impl: 业务实现类
vo: 返回给前端的数据对象
```

## 5. 第一阶段接口开发顺序

建议先做只读展示接口。

第一批接口：

```text
GET /api/majors
GET /api/majors/{id}
GET /api/majors/{id}/courses
GET /api/courses/{id}/knowledge-points
```

第二批接口：

```text
GET /api/careers
GET /api/careers/{id}
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

第三批接口：

```text
GET /api/careers/{id}/learning-path
GET /api/majors/{id}/overview
```

第一阶段不要急着写新增、修改、删除接口。先保证查询链路稳定。

## 6. 重要 SQL 设计原则

`course` 表现在是课程基础表，不再保存 `major_id`。

错误写法：

```sql
SELECT *
FROM course
WHERE major_id = ?;
```

正确写法：

```sql
SELECT c.*
FROM major_course mc
JOIN course c ON mc.course_id = c.id
WHERE mc.major_id = ?
ORDER BY mc.type, mc.id;
```

如果要查专业课程时同时返回专业侧字段，例如课程类型、学分：

```sql
SELECT
  c.id,
  c.name,
  c.description,
  mc.type,
  mc.credits
FROM major_course mc
JOIN course c ON mc.course_id = c.id
WHERE mc.major_id = ?
ORDER BY mc.type, mc.id;
```

课程知识点查询：

```sql
SELECT
  id,
  course_id,
  name,
  description,
  difficulty,
  ability,
  source
FROM knowledge_point
WHERE course_id = ?
ORDER BY id;
```

职业相关课程查询：

```sql
SELECT
  c.id,
  c.name,
  c.description,
  cc.course_role,
  cc.related_ability,
  cc.related_points,
  cc.importance,
  cc.is_core,
  cc.learning_stage
FROM career_course cc
JOIN course c ON cc.course_id = c.id
WHERE cc.career_id = ?
ORDER BY cc.is_core DESC, cc.learning_stage, cc.id;
```

职业能力查询：

```sql
SELECT
  id,
  career_id,
  name,
  description,
  ability_type,
  importance,
  learning_advice
FROM career_ability
WHERE career_id = ?
ORDER BY id;
```

职业知识点查询：

```sql
SELECT
  ckp.id,
  ckp.career_id,
  ckp.course_id,
  ckp.knowledge_point_id,
  kp.name AS knowledge_point_name,
  kp.description AS knowledge_point_description,
  c.name AS course_name,
  ckp.use_scene,
  ckp.importance,
  ckp.learning_advice,
  ckp.source
FROM career_knowledge_point ckp
JOIN knowledge_point kp ON ckp.knowledge_point_id = kp.id
JOIN course c ON ckp.course_id = c.id
WHERE ckp.career_id = ?
ORDER BY ckp.importance DESC, ckp.id;
```

## 7. 统一返回格式

建议 Controller 统一返回：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

空列表示例：

```json
{
  "code": 200,
  "message": "success",
  "data": []
}
```

不要因为某个职业暂时没有能力或知识点就返回错误。

## 8. 实体类设计建议

实体类字段尽量和数据库字段对应。

例如 `Course`：

```text
id
name
description
createdAt
updatedAt
```

例如 `MajorCourse`：

```text
id
majorId
courseId
type
credits
createdAt
updatedAt
```

返回给前端时，可以使用 VO 合并字段。

例如专业课程 VO：

```text
id
name
description
type
credits
```

这里的 `type` 和 `credits` 来自 `major_course`，不是来自 `course`。

## 9. MyBatis 开发规范

建议：

```text
Mapper 接口只定义数据库操作方法
复杂 SQL 写在 XML 中
Service 负责组织业务逻辑
Controller 只负责接收请求和返回结果
```

不要在 Controller 中直接写 SQL。

不要让前端直接关心数据库表结构。

## 10. 前端适配注意事项

当前前端仍主要使用 `fronted/src/data/majors.js` 中的本地数据。

后续接后端接口时，重点页面是：

```text
fronted/src/views/MajorView.vue
fronted/src/components/CourseModal.vue
fronted/src/components/JobModal.vue
fronted/src/views/RelationMapView.vue
fronted/src/views/ResourceLibraryView.vue
```

前端当前课程展示需要的字段大致包括：

```text
id
name
type
credits
description
skills
jobs
points
interaction
aiHint
```

第一阶段后端不一定全部提供这些字段，可以先提供：

```text
id
name
type
credits
description
knowledgePoints
```

职业相关字段后续再补。

## 11. 当前优先级

优先级从高到低：

```text
1. 后端项目能启动
2. 数据库连接成功
3. 专业列表能查
4. 专业课程能查
5. 课程知识点能查
6. 职业列表能查
7. 职业课程能查
8. 职业能力和职业知识点能查
9. 前端再逐步接接口
```

## 12. 开发注意事项

1. 查询专业课程必须通过 `major_course JOIN course`。
2. 查询职业课程必须通过 `career_course JOIN course`。
3. 查询职业知识点建议通过 `career_knowledge_point JOIN knowledge_point JOIN course`。
4. 数据缺失时返回空数组，不要返回错误。
5. 暂时不要删除 `course_backup_before_rebuild`。
6. 暂时不要重构前端页面。
7. 先做只读接口，再考虑管理端增删改。
8. 每次新增接口后，先用浏览器、Postman、IDEA HTTP Client 或 curl 验证。

## 13. 推荐下一步

下一步建议在 IDEA 中创建 Spring Boot Maven 项目，然后先实现：

```text
GET /api/majors
GET /api/majors/{id}/courses
GET /api/courses/{id}/knowledge-points
```

这三个接口跑通后，后端基础链路就建立起来了。
