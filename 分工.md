# smart_learning 后端开发分工说明

## 一、当前开发目标

本阶段开始进行 `smart_learning` 项目的后端开发。

第一阶段后端开发目标：

```text
先完成数据库中已有数据的查询接口，保证前端能够从后端获取专业、课程、知识点、职业、职业能力、职业课程、职业知识点等数据。
```

当前阶段暂时不做复杂推荐算法，也不急着做新增、修改、删除接口。

优先完成以下事情：

1. 后端项目能够正常启动；
2. 后端能够成功连接 MySQL 数据库；
3. 专业、课程、知识点数据能够正常查询；
4. 职业、职业能力、职业课程、职业知识点数据能够正常查询；
5. 接口返回格式统一，方便前端后续调用。

---

## 二、总体分工原则

本次后端由两个人共同完成，建议按照业务模块进行分工，而不是按照 Controller、Service、Mapper 分层拆分。

不建议这样分：

```text
一个人只写 Controller
一个人只写 Service 和 Mapper
```

原因是一个接口通常会同时涉及：

```text
Controller → Service → Mapper → XML SQL → VO
```

如果按层拆分，两个人会频繁修改同一批代码，容易产生冲突，也不利于独立测试。

建议按照以下方式分工：

```text
成员 A：后端基础框架 + 专业课程模块
成员 B：职业相关模块
```

---

## 三、成员 A 分工：后端基础框架 + 专业课程模块

### 1. 主要职责

成员 A 主要负责搭建后端项目基础结构，并完成专业、课程、知识点相关接口。

### 2. 具体任务

#### 任务 1：创建 Spring Boot 后端项目

建议使用以下配置：

```text
JDK：11
Spring Boot：2.7.x
构建工具：Maven
数据库：MySQL 8.0
持久层：MyBatis
```

建议引入依赖：

```text
Spring Web
MyBatis Framework
MySQL Driver
Lombok
```

建议项目信息：

```text
Group：com.example
Artifact：smart-learning-backend
Package：com.example.smartlearning
Packaging：Jar
Java：11
```

---

#### 任务 2：建立后端基础目录结构

建议目录结构如下：

```text
backend
└── src
    └── main
        ├── java
        │   └── com.example.smartlearning
        │       ├── SmartLearningApplication.java
        │       ├── common
        │       ├── config
        │       ├── controller
        │       ├── dto
        │       ├── entity
        │       ├── mapper
        │       ├── service
        │       ├── service.impl
        │       └── vo
        └── resources
            ├── application.yml
            └── mapper
```

各目录作用：

| 目录 | 作用 |
|---|---|
| `common` | 通用返回结果、常量、工具类 |
| `config` | 配置类 |
| `controller` | 接收前端请求 |
| `dto` | 接收前端提交的数据 |
| `entity` | 数据库实体类 |
| `mapper` | MyBatis Mapper 接口 |
| `resources/mapper` | MyBatis XML SQL 文件 |
| `service` | 业务接口 |
| `service.impl` | 业务实现类 |
| `vo` | 返回给前端的数据对象 |

---

#### 任务 3：配置数据库连接

在 `application.yml` 中配置 MySQL 数据库连接。

数据库名：

```text
smart_learning
```

需要保证：

```text
1. 项目可以正常启动；
2. 后端可以成功连接数据库；
3. MyBatis 能够扫描到 Mapper 和 XML 文件。
```

---

#### 任务 4：编写统一返回结果类

建议统一返回格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

例如可以创建：

```text
common/Result.java
```

要求：

```text
1. 查询成功统一返回 code = 200；
2. 查询结果为空时返回空数组 []；
3. 不要因为某些数据暂时缺失就返回 500 错误。
```

---

#### 任务 5：实现专业相关接口

成员 A 负责实现以下接口：

```text
GET /api/majors
GET /api/majors/{id}
GET /api/majors/{id}/courses
GET /api/courses/{id}/knowledge-points
```

涉及数据表：

```text
major
major_course
course
knowledge_point
```

---

### 3. 重点 SQL 注意事项

当前 `course` 表已经重构为课程基础表，不再保存 `major_id`。

因此，查询某个专业的课程时，不能再写：

```sql
SELECT *
FROM course
WHERE major_id = ?;
```

正确写法应为：

```sql
SELECT
    c.id,
    c.name,
    c.description,
    mc.type,
    mc.credits
FROM major_course mc
JOIN course c ON mc.course_id = c.id
WHERE mc.major_id = ?
ORDER BY mc.type, mc.id;
```

课程知识点查询：

```sql
SELECT
    id,
    course_id,
    name,
    description,
    difficulty,
    ability,
    source
FROM knowledge_point
WHERE course_id = ?
ORDER BY id;
```

---

## 四、成员 B 分工：职业相关模块

### 1. 主要职责

成员 B 主要负责职业相关接口，包括职业列表、职业详情、职业课程、职业能力和职业知识点。

成员 B 应在成员 A 搭好项目基础结构后，从主分支或开发分支拉取代码，再开始开发职业模块。

---

### 2. 具体任务

#### 任务 1：编写职业相关实体类

建议编写以下实体类：

```text
Career
CareerCourse
CareerAbility
CareerKnowledgePoint
```

涉及数据表：

```text
career
career_course
career_ability
career_knowledge_point
```

---

#### 任务 2：编写职业相关 VO

由于职业相关接口经常需要多表联查，所以建议不要直接把数据库表结构返回给前端，而是使用 VO。

建议编写：

```text
CareerCourseVO
CareerAbilityVO
CareerKnowledgePointVO
```

例如职业课程接口需要返回：

```text
课程ID
课程名称
课程简介
课程作用
对应能力
对应知识点
重要程度
是否核心课程
学习阶段
```

这些字段来自：

```text
career_course JOIN course
```

---

#### 任务 3：实现职业相关接口

成员 B 负责实现以下接口：

```text
GET /api/careers
GET /api/careers/{id}
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

涉及数据表：

```text
career
career_course
career_ability
career_knowledge_point
knowledge_point
course
```

---

### 3. 重点 SQL 注意事项

职业相关课程查询：

```sql
SELECT
    c.id,
    c.name,
    c.description,
    cc.course_role,
    cc.related_ability,
    cc.related_points,
    cc.importance,
    cc.is_core,
    cc.learning_stage
FROM career_course cc
JOIN course c ON cc.course_id = c.id
WHERE cc.career_id = ?
ORDER BY cc.is_core DESC, cc.learning_stage, cc.id;
```

职业能力查询：

```sql
SELECT
    id,
    career_id,
    name,
    description,
    ability_type,
    importance,
    learning_advice
FROM career_ability
WHERE career_id = ?
ORDER BY id;
```

职业知识点查询：

```sql
SELECT
    ckp.id,
    ckp.career_id,
    ckp.course_id,
    ckp.knowledge_point_id,
    kp.name AS knowledge_point_name,
    kp.description AS knowledge_point_description,
    c.name AS course_name,
    ckp.use_scene,
    ckp.importance,
    ckp.learning_advice,
    ckp.source
FROM career_knowledge_point ckp
JOIN knowledge_point kp ON ckp.knowledge_point_id = kp.id
JOIN course c ON ckp.course_id = c.id
WHERE ckp.career_id = ?
ORDER BY ckp.importance DESC, ckp.id;
```

---

## 五、接口开发优先级

### 第一优先级：项目能跑通

由成员 A 负责：

```text
1. 项目启动成功
2. 数据库连接成功
3. 统一返回格式完成
```

---

### 第二优先级：专业课程链路

由成员 A 负责：

```text
GET /api/majors
GET /api/majors/{id}/courses
GET /api/courses/{id}/knowledge-points
```

目标：

```text
专业 → 课程 → 知识点
```

这条链路必须最先跑通，因为前端专业页和课程详情页会优先用到。

---

### 第三优先级：职业相关链路

由成员 B 负责：

```text
GET /api/careers
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

目标：

```text
职业 → 课程
职业 → 能力
职业 → 知识点
```

这条链路用于职业详情页、职业推荐页和关系图页面。

---

## 六、接口测试要求

每完成一个接口，都必须进行测试。

可以使用：

```text
浏览器
Postman
IDEA HTTP Client
curl
```

每个接口至少检查以下内容：

```text
1. 接口是否能正常访问；
2. 返回格式是否统一；
3. data 字段是否正常；
4. 查询不到数据时是否返回空数组；
5. 是否出现 500 错误；
6. SQL 是否查错表或字段；
7. 前端需要的字段是否基本具备。
```

---

## 七、Git 协作建议

建议至少保留两个主要分支：

```text
main：稳定版本
dev：日常开发版本
```

两个人分别创建自己的功能分支：

```text
feature/major-course-api
feature/career-api
```

推荐流程：

```text
1. 成员 A 先创建后端基础项目；
2. 成员 A 将基础项目提交到 dev 分支；
3. 成员 B 从 dev 分支拉取最新代码；
4. 成员 A 创建 feature/major-course-api 分支；
5. 成员 B 创建 feature/career-api 分支；
6. 两个人分别开发自己的模块；
7. 开发完成并测试通过后，再合并回 dev 分支。
```

提交信息建议清楚一点，例如：

```text
feat: 初始化后端项目结构
feat: 完成专业课程查询接口
feat: 完成课程知识点查询接口
feat: 完成职业列表查询接口
feat: 完成职业能力查询接口
fix: 修复专业课程查询 SQL
```

---

## 八、共同开发规范

### 1. Controller 不直接写 SQL

错误做法：

```text
Controller 中直接写数据库查询逻辑
```

正确做法：

```text
Controller 调用 Service
Service 调用 Mapper
Mapper 对应 XML SQL
```

---

### 2. SQL 尽量写在 XML 文件中

建议：

```text
简单 SQL 可以用注解
复杂 SQL 写在 resources/mapper/*.xml 中
```

本项目涉及较多 JOIN 查询，因此建议统一写 XML。

---

### 3. 返回给前端尽量使用 VO

数据库实体类主要对应数据库表。

前端需要的数据可以使用 VO 重新组织。

例如：

```text
MajorCourseVO
CareerCourseVO
CareerKnowledgePointVO
```

这样前端不用关心数据库中字段到底来自哪张表。

---

### 4. 空数据不能报错

当前数据库中部分职业能力和职业知识点还没有补齐。

例如某些职业暂时没有：

```text
career_ability
career_knowledge_point
```

此时接口应该返回：

```json
{
  "code": 200,
  "message": "success",
  "data": []
}
```

不能返回错误。

---

### 5. 暂时不做增删改接口

第一阶段只做查询接口。

暂时不做：

```text
新增专业
修改课程
删除职业
后台管理系统
复杂推荐算法
```

等展示接口全部跑通后，再考虑管理端或推荐逻辑。

---

## 九、当前推荐任务清单

### 成员 A 今日任务清单

```text
[ ] 创建 Spring Boot Maven 项目
[ ] 配置 MySQL 数据库连接
[ ] 配置 MyBatis Mapper 扫描
[ ] 创建 common/Result.java
[ ] 创建基础目录结构
[ ] 创建 Major 实体类
[ ] 创建 Course 实体类
[ ] 创建 KnowledgePoint 实体类
[ ] 实现 GET /api/majors
[ ] 实现 GET /api/majors/{id}
[ ] 实现 GET /api/majors/{id}/courses
[ ] 实现 GET /api/courses/{id}/knowledge-points
[ ] 测试专业课程链路
```

---

### 成员 B 今日任务清单

```text
[ ] 从 dev 分支拉取成员 A 创建好的后端项目
[ ] 创建 Career 实体类
[ ] 创建 CareerCourse 实体类
[ ] 创建 CareerAbility 实体类
[ ] 创建 CareerKnowledgePoint 实体类
[ ] 创建 CareerCourseVO
[ ] 创建 CareerAbilityVO
[ ] 创建 CareerKnowledgePointVO
[ ] 实现 GET /api/careers
[ ] 实现 GET /api/careers/{id}
[ ] 实现 GET /api/careers/{id}/courses
[ ] 实现 GET /api/careers/{id}/abilities
[ ] 实现 GET /api/careers/{id}/knowledge-points
[ ] 测试职业相关链路
```

---

## 十、阶段验收标准

当以下接口都能正常返回数据时，说明第一阶段后端基本完成：

```text
GET /api/majors
GET /api/majors/{id}
GET /api/majors/{id}/courses
GET /api/courses/{id}/knowledge-points

GET /api/careers
GET /api/careers/{id}
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

验收要求：

```text
1. 项目能正常启动；
2. 数据库能正常连接；
3. 接口返回统一格式；
4. 查询结果正确；
5. 空数据返回空数组；
6. 控制台无明显 SQL 报错；
7. 前端可以开始尝试接入接口。
```

---

## 十一、总结

本次后端分工采用“基础框架 + 专业课程模块”和“职业相关模块”两条线并行开发。

成员 A 先负责项目基础搭建和专业课程链路，保证项目能够启动、数据库能够连接、专业和课程相关接口能够正常查询。

成员 B 在基础项目完成后负责职业相关接口，完成职业、职业课程、职业能力和职业知识点数据查询。

两个人开发时要注意：

```text
少改同一个文件
接口完成后及时测试
SQL 查询要符合当前数据库结构
返回格式保持统一
空数据不要报错
```

第一阶段完成后，项目后端就可以支撑前端进行基础数据展示和接口联调。
