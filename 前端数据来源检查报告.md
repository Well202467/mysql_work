# 前端数据来源检查报告

## 一、检查结论

当前前端项目中，后台管理页面基本已经接入后端接口；普通展示页面仍主要使用 `fronted/src/data/majors.js` 中的本地静态数据。

因此存在一个明显问题：

> 后台管理界面已经可以维护专业、课程、知识点、职业等数据，但普通展示页面仍读取本地静态数据，后台修改后的数据不会同步展示到普通页面。

建议后续优先把普通展示页面逐步改为调用公开查询接口，不要直接调用 `/api/admin/*` 后台接口。

## 二、当前页面数据来源

| 页面 | 路由 | 文件 | 当前数据来源 |
| --- | --- | --- | --- |
| 首页 | `/` | `fronted/src/views/HomeView.vue` | 页面内静态文案，不调用后端 |
| 学科览要 | `/majors` | `fronted/src/views/MajorCatalogView.vue` | `src/data/majors.js` 的 `getAllMajors()` |
| 学科详情 | `/major/:id` | `fronted/src/views/MajorView.vue` | `src/data/majors.js` 的 `getMajorById()`、`getJobProfileByName()` |
| 学职图谱 | `/map` | `fronted/src/views/RelationMapView.vue` | `src/data/majors.js` 的 `getAllMajors()` |
| 博习斋 | `/resources` | `fronted/src/views/ResourceLibraryView.vue` | `src/data/majors.js` 的 `getAllMajors()`、`getSupportModules()` |
| 课程弹窗 | - | `fronted/src/components/CourseModal.vue` | 接收父页面传入的课程对象，本身不请求接口 |
| 岗位弹窗 | - | `fronted/src/components/JobModal.vue` | 接收父页面传入的岗位对象，本身不请求接口 |
| 管理员登录 | `/admin/login` | `fronted/src/views/admin/AdminLogin.vue` | `POST /api/admin/login` |
| 后台首页 | `/admin/dashboard` | `fronted/src/views/admin/AdminDashboard.vue` | 页面内静态入口列表 |
| 后台布局 | `/admin/*` | `fronted/src/views/admin/AdminLayout.vue` | 页面内静态导航列表 |
| 专业管理 | `/admin/majors` | `fronted/src/views/admin/AdminMajorManage.vue` | `/api/admin/majors` 增删改查 |
| 课程管理 | `/admin/courses` | `fronted/src/views/admin/AdminCourseManage.vue` | `/api/admin/courses` 增删改查 |
| 专业课程关系管理 | `/admin/major-courses` | `fronted/src/views/admin/AdminMajorCourseManage.vue` | `/api/admin/major-courses`，并调用专业、课程下拉选项 |
| 知识点管理 | `/admin/knowledge-points` | `fronted/src/views/admin/AdminKnowledgePointManage.vue` | `/api/admin/knowledge-points`，并调用课程下拉选项 |
| 职业管理 | `/admin/careers` | `fronted/src/views/admin/AdminCareerManage.vue` | `/api/admin/careers` 增删改查 |
| 职业画像管理 | `/admin/career-profiles` | `fronted/src/views/admin/AdminCareerProfileManage.vue` | `/api/admin/career-profiles`，并调用职业下拉选项 |
| 职业能力管理 | `/admin/career-abilities` | `fronted/src/views/admin/AdminCareerAbilityManage.vue` | `/api/admin/career-abilities`，并调用职业下拉选项 |
| 职业课程关系管理 | `/admin/career-courses` | `fronted/src/views/admin/AdminCareerCourseManage.vue` | `/api/admin/career-courses`，并调用职业、课程、能力、知识点选项 |
| 职业知识点关系管理 | `/admin/career-knowledge-points` | `fronted/src/views/admin/AdminCareerKnowledgePointManage.vue` | `/api/admin/career-knowledge-points`，并调用职业、课程、知识点选项 |

## 三、仍在使用本地静态数据的页面

本地静态数据集中在：

```text
fronted/src/data/majors.js
```

该文件目前包含：

- `jobProfiles`：岗位画像静态数据
- `supportModules`：学习支持模块静态数据
- `majors`：专业、课程、能力、岗位、资源、AI 提示等聚合静态数据
- `getMajorById(id)`
- `getAllMajors()`
- `getSupportModules()`
- `getAllJobProfiles()`
- `getJobProfileByName(name)`

仍在使用本地数据的页面包括：

| 页面 | 使用的本地方法 |
| --- | --- |
| `MajorCatalogView.vue` | `getAllMajors()` |
| `MajorView.vue` | `getMajorById()`、`getJobProfileByName()` |
| `RelationMapView.vue` | `getAllMajors()` |
| `ResourceLibraryView.vue` | `getAllMajors()`、`getSupportModules()` |

## 四、管理界面已接后端但展示页仍用本地数据的情况

存在这种情况。

后台已经接入后端的模块：

- 专业管理
- 课程管理
- 专业课程关系管理
- 知识点管理
- 职业管理
- 职业画像管理
- 职业能力管理
- 职业课程关系管理
- 职业知识点关系管理

但普通展示页仍从 `src/data/majors.js` 读取专业、课程、能力、岗位、资源等内容。

影响是：

- 管理员在后台新增或修改专业后，`/majors` 不会同步显示新数据。
- 管理员修改课程或专业课程关系后，`/major/:id` 的课程列表不会同步变化。
- 管理员维护职业、职业画像、职业能力后，普通页面中的岗位展示仍然来自本地静态数据。
- 知识点、职业课程关系、职业知识点关系虽然后台可维护，但普通展示页目前没有真正使用这些后端数据。

## 五、建议替换为的后端接口

### 1. 专业列表页 `/majors`

当前页面：

```text
fronted/src/views/MajorCatalogView.vue
```

当前数据来源：

```js
getAllMajors()
```

建议改为：

```http
GET /api/majors
```

用途：

- 查询专业列表
- 替代本地 `majors` 数组中的基础专业信息

注意：

当前页面还使用了 `shortTitle`、`tagline`、`abilityOverview`、`careerFocus` 等展示字段，而现有 `major` 表字段只有：

```text
id, name, intro, created_at, updated_at
```

如果继续保留这些丰富展示效果，需要后端补充聚合字段，或者前端先退化为只展示 `id`、`name`、`intro`。

### 2. 专业详情页 `/major/:id`

当前页面：

```text
fronted/src/views/MajorView.vue
```

当前数据来源：

```js
getMajorById(id)
getJobProfileByName(name)
```

建议改为组合调用：

```http
GET /api/majors/{id}
GET /api/majors/{id}/courses
GET /api/courses/{courseId}/knowledge-points
GET /api/careers
```

如果需要展示职业关联课程、职业能力、职业知识点，可继续调用：

```http
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

注意：

当前 `MajorView.vue` 页面展示内容比较丰富，包括：

- 专业简介
- 学习目标
- 核心能力
- 典型岗位方向
- 核心课程
- 下一步可读内容
- 课程弹窗
- 岗位弹窗

现有公开接口可以支撑一部分数据，但不能完全覆盖所有页面字段。

建议后端后续增加一个专业详情聚合接口，例如：

```http
GET /api/majors/{id}/overview
```

返回专业详情页需要的完整结构。

### 3. 学职图谱页 `/map`

当前页面：

```text
fronted/src/views/RelationMapView.vue
```

当前数据来源：

```js
getAllMajors()
```

建议改为：

```http
GET /api/majors
GET /api/majors/{id}/courses
GET /api/careers
```

如果需要更准确展示课程、能力、岗位、知识点之间的关系，建议新增聚合接口：

```http
GET /api/relation-map
```

或：

```http
GET /api/majors/{id}/relation-map
```

原因：

图谱页面本质上不是简单列表，而是专业、课程、能力、岗位、资源之间的组合关系。前端自己拼多个接口也可以，但页面逻辑会比较复杂，不适合初学阶段维护。

### 4. 博习斋 `/resources`

当前页面：

```text
fronted/src/views/ResourceLibraryView.vue
```

当前数据来源：

```js
getAllMajors()
getSupportModules()
```

建议接口：

```http
GET /api/resources
GET /api/majors/{id}/resources
```

目前后端公开接口中没有看到资源管理或资源查询接口。

如果该页面需要继续保留，建议后端后续新增资源表和资源接口，例如：

资源表可考虑：

```text
resource
id, name, type, format, description, source_url, created_at, updated_at
```

资源与课程关系表可考虑：

```text
resource_course
id, resource_id, course_id
```

## 六、现有公开查询接口

当前后端已经存在的普通展示查询接口：

```http
GET /api/majors
GET /api/majors/{id}
GET /api/majors/{id}/courses
GET /api/courses/{id}/knowledge-points
GET /api/careers
GET /api/careers/{id}
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

当前后端已经存在的后台管理接口：

```http
POST   /api/admin/login
GET    /api/admin/majors
POST   /api/admin/majors
PUT    /api/admin/majors/{id}
DELETE /api/admin/majors/{id}
GET    /api/admin/courses
POST   /api/admin/courses
PUT    /api/admin/courses/{id}
DELETE /api/admin/courses/{id}
GET    /api/admin/major-courses
POST   /api/admin/major-courses
PUT    /api/admin/major-courses/{id}
DELETE /api/admin/major-courses/{id}
GET    /api/admin/knowledge-points
POST   /api/admin/knowledge-points
PUT    /api/admin/knowledge-points/{id}
DELETE /api/admin/knowledge-points/{id}
GET    /api/admin/careers
POST   /api/admin/careers
PUT    /api/admin/careers/{id}
DELETE /api/admin/careers/{id}
GET    /api/admin/career-profiles
POST   /api/admin/career-profiles
PUT    /api/admin/career-profiles/{id}
DELETE /api/admin/career-profiles/{id}
GET    /api/admin/career-abilities
POST   /api/admin/career-abilities
PUT    /api/admin/career-abilities/{id}
DELETE /api/admin/career-abilities/{id}
GET    /api/admin/career-courses
POST   /api/admin/career-courses
PUT    /api/admin/career-courses/{id}
DELETE /api/admin/career-courses/{id}
GET    /api/admin/career-knowledge-points
POST   /api/admin/career-knowledge-points
PUT    /api/admin/career-knowledge-points/{id}
DELETE /api/admin/career-knowledge-points/{id}
GET    /api/admin/options/majors
GET    /api/admin/options/courses
GET    /api/admin/options/careers
GET    /api/admin/options/knowledge-points
```

## 七、修改建议

### 第一阶段：先替换专业列表和专业详情的核心数据

建议优先改：

```text
fronted/src/views/MajorCatalogView.vue
fronted/src/views/MajorView.vue
```

改造目标：

- `/majors` 从 `GET /api/majors` 获取专业列表。
- `/major/:id` 从 `GET /api/majors/{id}` 获取专业信息。
- `/major/:id` 从 `GET /api/majors/{id}/courses` 获取课程列表。

这样后台“专业管理”“课程管理”“专业课程关系管理”的数据变化，可以先同步到普通展示页。

### 第二阶段：替换职业和能力相关展示

建议继续改：

```text
fronted/src/views/MajorView.vue
fronted/src/views/RelationMapView.vue
```

可使用：

```http
GET /api/careers
GET /api/careers/{id}
GET /api/careers/{id}/courses
GET /api/careers/{id}/abilities
GET /api/careers/{id}/knowledge-points
```

### 第三阶段：处理资源页面

当前 `ResourceLibraryView.vue` 依赖本地 `supportModules` 和 `major.resources`。

如果后端暂时没有资源表，可以先保留本地静态数据。

如果希望彻底后端化，建议新增资源相关接口：

```http
GET /api/resources
GET /api/majors/{id}/resources
```

### 第四阶段：逐步删除或弱化 `src/data/majors.js`

当普通展示页都改成接口后，`src/data/majors.js` 可以：

- 先保留作为兜底数据；
- 或只保留少量前端展示配置；
- 最终不再作为业务数据来源。

## 八、建议优先级

| 优先级 | 修改内容 | 原因 |
| --- | --- | --- |
| 高 | `MajorCatalogView.vue` 改接 `GET /api/majors` | 专业管理后台已经完成，应同步到展示页 |
| 高 | `MajorView.vue` 改接 `GET /api/majors/{id}` 和 `GET /api/majors/{id}/courses` | 专业详情和课程列表是核心展示内容 |
| 中 | `RelationMapView.vue` 改接专业、课程、职业公开接口 | 图谱页需要真实关系数据 |
| 中 | `MajorView.vue` 的岗位弹窗改接职业/职业画像接口 | 后台职业相关模块已经完成 |
| 低 | `ResourceLibraryView.vue` 改接资源接口 | 当前后端暂未看到资源公开接口，需先补后端 |

## 九、总结

当前项目最大的数据来源不一致点是：

> 后台管理页已经后端化，普通展示页仍静态化。

建议下一步先不要一次性大改所有页面，而是从最核心的两个页面开始：

```text
fronted/src/views/MajorCatalogView.vue
fronted/src/views/MajorView.vue
```

先让专业列表、专业详情、专业课程列表真正读取数据库数据。这样前后端数据链路会更清晰，也更适合逐步学习和调试。
